function App() {
  return (
    <div style={{ textAlign: 'center', marginTop: '100px' }}>
      <h1>✅ GitHub Actions CI/CD Working!</h1>
      <p>This React app is tested and built automatically on every push.</p>
      <p>Try pushing new code to the main branch to trigger the workflow!</p>
    </div>
  );
}

export default App;
